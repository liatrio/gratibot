# dont run on CI
[ -n "$CI" ] && exit 0

printf "\033[1mPre-Commit: Linting\033[0m\n\n"
npm run lint || {
  printf "\n\033[0;33mLint failed. Run 'npm run lint:fix' to auto-fix issues, then stage the changes and commit again.\033[0m\n"
  exit 1
}
printf "\n────────────────────────────────────────\n\n"
