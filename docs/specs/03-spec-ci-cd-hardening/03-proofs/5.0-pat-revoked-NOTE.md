# 5.0-pat-revoked screenshot: unavailable

The operator revoked the `GRATIBOT_RELEASE_TOKEN` repository secret and the
underlying PAT during the Unit 7 post-merge handoff (Task 7.9) but did not
capture a screenshot of the Actions → Secrets tab showing the secret removed.

**Substitute evidence:** the absence of `GRATIBOT_RELEASE_TOKEN` in the
repository's Actions secrets can be confirmed at any time by a maintainer
running `gh secret list` against `liatrio/gratibot` from an authenticated
terminal. At the time this note was written, `gh secret list` on `main`
returned these secrets (no `GRATIBOT_RELEASE_TOKEN`):

- AZURE_CLIENT_ID
- AZURE_NONPROD_SUBSCRIPTION_ID
- AZURE_PROD_SUBSCRIPTION_ID
- CODE_CLIMATE_REPORTER_ID
- RELEASE_APP_PRIVATE_KEY

(`RELEASE_APP_ID` was moved to the variables tab during the post-merge
recovery described in the earlier "Create Release — Generate release token"
failure and its subsequent successful re-run.)

**Functional proof:** the post-merge `release.yml` run for commit
`5141d72` (URL recorded in `5.0-release-run.txt`) successfully minted a
GitHub App installation token and ran `semantic-release` end-to-end. The
next release workflow run will continue to succeed without the old PAT —
any regression here would surface as a red "Create Release" job on the
first subsequent push to `main`.

**Confirmation recorded at handoff:** the user confirmed Task 7.9 was
completed on 2026-04-16 by deleting the `GRATIBOT_RELEASE_TOKEN` secret
and revoking the underlying PAT.

If a screenshot is required for the validation phase, a maintainer can
capture one from the repository's secrets page at
`https://github.com/liatrio/gratibot/settings/secrets/actions` and add it
to this directory as `5.0-pat-revoked.png`.
